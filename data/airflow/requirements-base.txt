# ============================================
# Airflow Base Requirements - Minimal Set
# ============================================
# Use this for production deployments with minimal dependencies

# Core Airflow Providers
apache-airflow-providers-mysql>=5.0.0
apache-airflow-providers-postgres>=5.8.0
apache-airflow-providers-http>=4.5.0

# Essential Libraries
pydantic>=2.5.0
pydantic-settings>=2.1.0
psycopg2-binary>=2.9.7
requests>=2.31.0
httpx>=0.25.0
tenacity>=8.2.2
redis>=5.0.1
structlog>=23.2.0
prometheus-client>=0.19.0
python-dateutil>=2.8.2
python-dotenv>=1.0.0

# Cloud Storage (choose based on your provider)
boto3>=1.34.0  # AWS
# azure-storage-blob>=12.19.0  # Azure (uncomment if needed)
# google-cloud-storage>=2.14.0  # GCP (uncomment if needed)

# Security
cryptography>=41.0.3
PyJWT>=2.8.0

# Kubernetes
kubernetes>=28.1.0
pyyaml>=6.0.1














